package com.d3v.senior.project.chunk;


public class ChunkManager 
{
	/*//Chunk Size
	static byte TilesPerChunk = 16;
	//Tile Size
	static byte TileSize = 32;
	//Total Chunk Size
	static int ChunkSize = TilesPerChunk * TileSize;
	
	//
	ChunkGenerator chunk;
	
	//
	Rectangle chunkBounds;
	
	byte test;
	byte numberHorizontalChunks;
	byte numberVerticalChunks;
	
	public ChunkManager()
	{	
		//A boundary rectangle to help with chunk loading
		chunkBounds = new Rectangle();
		chunkBounds.x = ChunkX * ChunkSize;
		chunkBounds.y = ChunkY * ChunkSize;
		chunkBounds.width = ChunkSize;
		chunkBounds.height = ChunkSize;
		
		//A float array to store the tile values
		float[][] tiles = new float[16][16];
		
		chunk = new ChunkGenerator();
		tiles = chunk.genChunk(0,0);
	}*/	
}
